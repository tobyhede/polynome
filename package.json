{
  "name": "polynome",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A tiny, zero-dependency polyrhythm and polymeter metronome for the browser.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check metronome.js && node --check model.js && node --test && npm run bundle && npm run site",
    "bundle": "node scripts/bundle.mjs",
    "site": "node scripts/site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
